<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AgriTech" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorAccent">@color/primary_dark</item>
        <item name="fontFamily">@font/kanit_regular</item>
        <item name="android:textColor">@color/black</item>
        <item name="background">@color/background_light_green</item>
    </style>

    <style name="ChatBot.Theme.AgriTech" parent="Theme.Material3.Light">
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="colorAccent">@color/primary_dark</item>
        <item name="fontFamily">@font/kanit_regular</item>
    </style>

    <style name="BottomNavigationView" parent="@style/TextAppearance.AppCompat.Caption">
        <item name="android:textSize">11sp</item>
    </style>

    <style name="BottomNavigationView.Active" parent="@style/TextAppearance.AppCompat.Caption">
        <item name="android:textSize">13sp</item>
    </style>

    <style name="Theme.AgriTech" parent="Base.Theme.AgriTech" />

</resources>